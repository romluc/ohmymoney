import React from 'react';

import { Title, OuterContainer, Container, Separator } from './styles.js';

const Home = () => {
  return (
    <OuterContainer>
      <Container>
        <Title>My Money</Title>
        <Separator />
      </Container>
    </OuterContainer>
  );
};

export default Home;
